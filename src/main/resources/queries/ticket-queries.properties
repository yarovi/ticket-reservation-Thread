ticket.insert=INSERT INTO tickets (ticket_code, reservation_id, issued_at) VALUES (?, ?, ?) RETURNING id

ticket.find-by-reservation-id=SELECT id, ticket_code, reservation_id, issued_at FROM tickets WHERE reservation_id = ?
